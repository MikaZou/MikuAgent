<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 路线 A：手机只做渲染 + 采集 + 播放，AI 全部在 PC 上跑 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 按住说话：原生录音权限，不再受浏览器安全上下文限制 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- 拍照给 Miku 看 -->
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MikuAgent"
        android:hardwareAccelerated="true"
        android:usesCleartextTraffic="true">
        <!--
          usesCleartextTraffic 是**必须**的：PC 与手机走局域网明文
          http://<PC>:8765 与 ws://<PC>:8765/ws。原生 socket 不受浏览器
          mixed-content 规则约束，这也是选原生 I/O 的原因之一。
        -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout|smallestScreenSize|uiMode"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
